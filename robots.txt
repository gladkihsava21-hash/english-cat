# Пути к панели и админке здесь НЕ перечисляем: robots.txt читают в том
# числе для разведки, а Disallow — это указатель, а не запрет. Админка
# закрыта от индексации заголовком X-Robots-Tag (см. .htaccess) и уезжает
# на секретный адрес. Роботам достаточно знать, что API индексировать не надо.
User-agent: *
Disallow: /api/

Sitemap: https://wordcat.ru/sitemap.xml
